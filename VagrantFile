Vagrant.configure(2) do |config|
 config.vm.box = 'ubuntu/trusty64'
 config.vm.hostname = 'DESKTOP-33DQ2LA'
 config.vm.provision 'shell', path: './Scripts/transferArchivesAndDataBaseToMachine2.sh'
 config.vm.provider :virtualbox do |v|
  v.name = 'M1.clone'
  v.gui = true
 end
end
